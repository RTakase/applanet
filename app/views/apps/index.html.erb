<%#= javascript_include_tag "application" %>
<%#= stylesheet_link_tag "application" %>

<%= content_for(:heading) do %>
  似ているアンドロイドアプリを探しましょう。<br>
  「このアプリと」似ているアイコンのアプリが知りたいけど検索できない・・・<br>
  「このアプリと」同じような機能のアプリが欲しいけど有名なやつしか検索できない・・・<br>
  そんなニッチなニーズを満たすべく、このページはアプリでアプリを検索します。<br><br>
  <span style="font-size:70%;">
    似ているアプリが検索できるようになってきました！合わせてとても重くなりました。
  </span>
  <i class="fa fa-child"></i>
<% end %>

<%= content_for(:howto) do %>
  手順：
  <ul class="fa-ul">
    <li><i class="fa-li fa fa-child"></i>
      「これと似ているアプリが欲しい！」のこれの名前をテキストボックスに入力する（あいまい検索が可能です）
    </li>
    <li><i class="fa-li fa fa-child"></i>
      似ているの基準を選択する
    </li>
    <li><i class="fa-li fa fa-child"></i>
      検索ボタンを押す
    </li>
  </ul>

  グラフについて：
  <ul class="fa-ul">
    <li><i class="fa-li fa fa-child"></i>
      グラフ上では、「似ている」アプリほど大きな円で描かれます。
    </li>
    <li><i class="fa-li fa fa-child"></i>
      ごちゃごちゃしている箇所は、ドラッグアンドドロップでズームしてみましょう。元に戻るにはダブルクリックです。
    </li>
    <li><i class="fa-li fa fa-child"></i>
      円をクリックすると、対応したアプリのGooglePlayページへ遷移します。帰ってきてくださいね。
    </li>
  </ul>
<% end %>

<%= content_for(:content) do %>

  <div class="container-fluid">

    <div class="row content">   <!– グリッドを使用 –>
      <div class="col-xs-12 col-md-3 form">
        <%= render "form" %>
      </div>
      <div class="col-xs-12 col-md-6 graph">
        <%= render "graph" %>
      </div>
      <div class="col-xs-12 col-md-3 applist">     
        <%#= render "applist" %>
      </div>
    </div>
  </div>

  <%#= render "nowloading" %>

<% end %>

<%= content_for(:data) do %>
  
  <div id="appdata">hoge</div>
<% end %>


